server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:flights
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  booking-window-minutes: 45
  hold-minutes: 10
  sweep-ms: 60000
